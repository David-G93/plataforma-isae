name: tests

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: plataforma_isae_dev
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: '8.4'
          tools: composer:v2
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'

      - name: Setup Application
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3307
          DB_DATABASE: plataforma_isae_dev
          DB_USERNAME: root
          DB_PASSWORD: root
        run: composer setup

      - name: Create test database
        run: mysql --host=127.0.0.1 --port=3307 --user=root --password=root -e "CREATE DATABASE plataforma_isae_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci"

      - name: Run CI Checks
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3307
          DB_DATABASE: plataforma_isae_test
          DB_USERNAME: root
          DB_PASSWORD: root
        run: composer ci:check
